import { RaceLocationPipe } from './race-location.pipe';

describe('RaceLocationPipe', () => {
  it('create an instance', () => {
    const pipe = new RaceLocationPipe();
    expect(pipe).toBeTruthy();
  });
});
